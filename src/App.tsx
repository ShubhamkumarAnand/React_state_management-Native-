import { useReducer } from 'react';

// ~ Reducer Function in JavaScript
// const list = [12, 23, 45];
// const total = list.reduce((curr, n) => curr + n);
// console.log(total);

function App() {
	return <div></div>;
}

export default App;
